{
    "name": "阿里百炼（通义千问）配置示例",
    "description": "使用阿里云的通义千问大模型服务",
    "config": {
        "model_type": "dashscope",
        "model": "qwen-plus",
        "api_key": "${DASHSCOPE_API_KEY}",
        "temperature": 0.7,
        "max_tokens": 4000,
        "extra_params": {
            "top_p": 0.8
        }
    }
}
